//print factorial of n example 5.

#include<stdio.h>
int fact(int n);//function declared
int main(){
	printf("factorial is %d",fact(5));
	return 0;
}
int fact(int n){
	if(n==0){
		return 1;//base case
	}
	int factnm1=fact(n-1);//function defining
	int factn=factnm1*n;
	return factn;
}
